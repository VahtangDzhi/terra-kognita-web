# Terra Kognita Railway Deploy

Static Terra Kognita website packaged for Railway using nginx.

Deploy options:

1. Create a new Railway project.
2. Deploy this folder as a Docker project.
3. Railway will build `Dockerfile` and serve `index.html`.
